# 이 저장소에서 에이전트가 지킬 것

이 프로젝트를 쓰는 사람은 **개발을 모릅니다.** 터미널을 열 줄 모르고, 파일 편집기도
못 엽니다. 그것을 전제로 움직이세요.

- **명령줄을 사용자에게 보여주지 마세요.** 실행이 필요하면 당신이 직접 실행합니다.
- 사용자가 무언가 눌러야 한다면 **웹 주소 한 줄**만 주세요. 그것이 유일하게 허용된
  요청입니다.
- 서버에서 웹 화면을 띄웠으면 **당신의 포트 전달 기능(웹 미리보기)** 으로 이어
  주세요. 로그에 `FORWARD_PORT=숫자` 가 찍혀 있습니다.
- 로그에 `ssh` 나 방화벽 이야기가 나오면 그건 **당신에게** 하는 말입니다. 사용자에게
  옮기지 마세요. 당신이 못 하면 "제 쪽에서 안 됩니다"라고만 말하세요.
- **키나 비밀번호는 묻지도, 대신 넣지도 마세요.** 사용자가 화면에서 직접 넣습니다.
- `.env` 는 절대 고치지 마세요. 계좌 전환은 설정 화면에서만 합니다.

## 사용자가 이 말을 하면

| 사용자가 치는 말 | 하는 일 |
|---|---|
| **실전투자** | 아래 「실전투자」 절차 |
| **모의투자** | `python setup.py` 를 띄우고 주소를 준 뒤, 1단계에서 모의투자로 되돌리라고 안내 |
| **현황** / **얼마나 벌었어** | `board.py` 를 띄우고(이미 떠 있으면 그대로) 주소만 주기 |
| **설정** | `python setup.py` 를 띄우고 주소만 주기 |

## 「실전투자」 절차

사용자가 **실전투자**라고 하면, 실제 돈으로 넘어가겠다는 뜻입니다.

1. `git pull` 로 최신 코드를 받습니다. `strategy.py` 가 충돌하면 사용자의 종목·금액·
   `INSTRUCTIONS` 는 그대로 두고 주석만 새것으로 맞춥니다.
2. `python -m unittest discover -s tests` 와 `python check.py` 를 돌립니다.
   **하나라도 실패하면 거기서 멈추고 사용자에게 알립니다.** 실제 돈이 걸린 일입니다.
3. `python trade.py --account` 로 지금 한도를 확인해서, 아래를 **숫자로** 보여 줍니다.

   > 지금 설정이라면 한 종목에 얼마, 최대 몇 종목, 최대 얼마까지 들어갑니다.
   > 손절은 몇 %, 익절은 몇 %입니다.

   그리고 **눈에 걸리는 것이 있으면 한 줄씩 짚어 줍니다.** 고치라고 시키지 말고,
   무슨 뜻인지만 알려 주세요. 판단은 사용자가 합니다.

   - 익절이 손절보다 작으면 → "이기는 거래 하나가 지는 거래 하나보다 작습니다.
     그만큼 자주 이겨야 본전입니다."
   - 프리마켓·애프터마켓이 켜져 있으면 → "사려는 사람이 적은 시간대라 원하는 값에
     안 걸리거나 불리하게 걸립니다. 지표는 아직 어제 것입니다."
   - 최대 한도가 사용자가 말한 예산보다 크면 → 그 차이를 그대로 말해 줍니다.

4. 그다음 **반드시 이것을 묻습니다.**

   > 실제 돈으로 하기 전에, 전략을 어떻게 할까요?
   >   ① 지금 전략 그대로 갑니다
   >   ② 실제 돈 기준으로 상담을 다시 합니다 (금액·손절선·시간대를 다시 정함)

   ②를 고르면 **설정 화면을 띄우기 전에** 상담부터 합니다. 상담 방식은 `setup.py`
   안의 프롬프트와 같되, 시작할 때 이 세 가지를 분명히 하세요.

   - **모의투자 때 쓰던 금액은 잊으라고 하세요.** 가짜 돈 5억에 맞춰 정한 숫자가
     그대로 남아 있으면 실제 계좌에서 너무 큽니다.
   - **"잃어도 생활에 지장 없는 금액"이 얼마인지** 먼저 묻고, 거기서 한 종목
     금액과 최대 종목 수를 거꾸로 계산하세요.
   - 프리마켓·애프터마켓을 계속할지 다시 확인하세요(`US_SESSIONS`).

   상담이 끝나면 `strategy.py` 를 고치고, `python check.py` 와
   `python -m unittest discover -s tests` 를 **둘 다 통과시킨 뒤** 바뀐 한도를
   다시 숫자로 보여 주고 "이대로 실제 돈으로 하시겠어요?"라고 물으세요.

5. 사용자가 좋다고 하면 그때 설정 화면을 띄웁니다.

   ```
   nohup .venv/bin/python setup.py --live > setup.log 2>&1 &
   ```

   `setup.log` 의 `FORWARD_PORT=숫자` 를 포트 전달로 열고 **주소만** 줍니다.
   `--live` 로 열면 화면이 순서(① Telegram ② 실제 계좌)를 스스로 안내합니다.
6. 사용자에게 이렇게만 말합니다.

   > 이 주소에서 ① Telegram을 먼저 연결하고 ② [실제 계좌]를 고른 뒤 저장하세요.
   > 키는 다시 넣지 않아도 됩니다. 다 되면 "다 했다"고 말해 주세요.

7. 사용자가 "다 했다"고 하면 `python trade.py --account` 로 `실제 계좌` 로 바뀌었는지
   확인하고, 바뀌었으면 알려 줍니다. 안 바뀌었으면 무엇이 남았는지 짚어 줍니다.
8. 마지막으로 이것을 알려 줍니다.

   > 이제부터 주문마다 Telegram으로 확인이 갑니다. **3분 안에 승인하지 않으면
   > 취소됩니다.** 손절·익절은 승인 없이 규칙이 바로 팝니다.
   > 되돌리려면 "모의투자"라고 말씀하세요.

**금액을 당신이 임의로 바꾸지 마세요.** 사용자가 줄여 달라고 하면 그때만 `strategy.py`
의 `BUY_AMOUNT` · `US_BUY_AMOUNT` · `MAX_HOLDINGS` 를 고치고, 고친 뒤 `python check.py`
를 통과시키세요.

## 예약(scheduled task)

예약 칸에는 내용을 붙여넣지 않습니다. 이 두 줄만 넣습니다.

```
이 폴더의 schedule.txt 파일을 읽고, 거기 적힌 대로 그대로 해.

그 파일에 없는 일은 하지 마. 파일을 못 찾으면 아무것도 하지 말고
"schedule.txt 를 못 찾았다"고만 알려 줘.
```

그래야 시키는 내용이 바뀌어도 예약을 다시 고칠 필요가 없습니다.
